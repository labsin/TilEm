 * keymap defined through tiemu compatible text files
 * connection management UI [external and virtual]
 * debugging :
	* breakpoints management
	* instruction stepping
	* pretty registers display (allow modifications)
	* disassembly
 * settings UI
 * screen capture (static and animated)
 * ROM dumping
 * calc 2 calc linking (physical) [i.e between a real calc and an emulated one] (necessary?)

